namespace "Silver.UI"

ifexp "C" {
    import extern "GLFW/glfw3.h"
    ifp "Windows" {
        import extern "glew.h"
    }

    public class Window {

        private GLFWwindow* window;
    
    }
}